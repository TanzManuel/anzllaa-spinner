import {makeToken,json} from './_lib.js';export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{ok:false,error:'Method tidak diizinkan.'});if(!process.env.ADMIN_PASSWORD)return json(res,500,{ok:false,error:'ADMIN_PASSWORD belum diatur di Vercel.'});if(String(req.body?.password||'')!==process.env.ADMIN_PASSWORD)return json(res,401,{ok:false,error:'Password admin salah.'});const token=makeToken();res.setHeader('Set-Cookie',`admin_session=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=43200`);return json(res,200,{ok:true})}
