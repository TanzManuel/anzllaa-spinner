import {redis,requireAdmin,json} from './_lib.js';export default async function handler(req,res){if(!requireAdmin(req,res))return;if(req.method!=='POST')return json(res,405,{ok:false,error:'Method tidak diizinkan.'});const codes=await redis.smembers('spin:available');if(codes.length)await Promise.all(codes.map(code=>redis.del(`spin:code:${code}`)));await redis.del('spin:available');return json(res,200,{ok:true,count:codes.length})}